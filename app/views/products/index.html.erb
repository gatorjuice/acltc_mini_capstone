<% @products.each do |product| %>

<div class="product">
  <a href="" class="product_link">
    <%= image_tag(product.image) %>
    <h3><%= product.name %></h3>
    <h4>$<%= product.price %></h4>
    <p><%= product.description %></p>
  </a>
</div>

<% end %>