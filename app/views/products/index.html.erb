<% @products.each do |product| %>
  <div class="product">
    <%= image_tag(product.image) %>
    <h3><%= product.name %></h3>
    <h4>$<%= product.price %></h4>
    <p><%= product.description %></p>
  </div>
<% end %>